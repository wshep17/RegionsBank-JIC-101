function calculateMonthlyPayment(inputs) {
  const {
    purchasePrice,
    cashBack,
    taxRate,
    tradeInValue,
    tradeInOwed,
    loanTerm,
    interestRate,
    downPayment
  } = inputs;
  return 0;
}

export { calculateMonthlyPayment };